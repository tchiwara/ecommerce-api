spring:
  application:
    name: ecommerce.api
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: true
  flyway:
    enabled: true
  jwt:
    secret: ${JWT_SECRET}
  config:
    import: optional:file:.env[.properties]


